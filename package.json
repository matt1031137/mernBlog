{
  "name": "blogapp",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install --legacy-peer-deps --prefix backend && npm install --legacy-peer-deps --prefix client && npm install --legacy-peer-deps vite --prefix client && cd client && npx vite build",
    "start": "npm run start --prefix backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}